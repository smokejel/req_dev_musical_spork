# Anthropic API Key for Claude
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# OpenAI API Key (for GPT-4o fallback)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_key_here

# Google API Key for Gemini (Primary Extract model - Phase 4.1)
# Get your key from: https://aistudio.google.com/app/apikey
# Required for large document processing (2M token context window)
GOOGLE_API_KEY=your_google_key_here

# LangSmith Tracing (Phase 5.1 - Enhanced Observability)
# Enable for precise token tracking, cost analysis, and debugging
# Get your key from: https://smith.langchain.com/
# HIGHLY RECOMMENDED for production use
LANGCHAIN_TRACING_V2=false
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com
LANGCHAIN_API_KEY=your_langsmith_key_here
LANGCHAIN_PROJECT=requirements-decomposition

# Cost Tracking & Budget Management (Phase 5.1)
# Set to 'true' to enable cost warnings when approaching budget
COST_TRACKING_ENABLED=true
COST_BUDGET_WARNING_THRESHOLD=1.00  # Warn when approaching $1.00 per run
COST_BUDGET_MAX=5.00  # Stop execution if cost exceeds $5.00

# Quality Dimension Weighting (Phase 7.3 - Domain-Aware Requirements)
# Configure weights for each quality dimension (must sum to 1.0)
# Default: Equal weighting (0.25 for 4 dimensions, 0.20 for 5 dimensions)
# Set to customize importance of specific dimensions
QUALITY_WEIGHT_COMPLETENESS=0.25  # Generic: 0.25, Domain-Aware: 0.20
QUALITY_WEIGHT_CLARITY=0.25        # Generic: 0.25, Domain-Aware: 0.20
QUALITY_WEIGHT_TESTABILITY=0.25    # Generic: 0.25, Domain-Aware: 0.20
QUALITY_WEIGHT_TRACEABILITY=0.25   # Generic: 0.25, Domain-Aware: 0.20
QUALITY_WEIGHT_DOMAIN_COMPLIANCE=0.20  # Only for domain-aware (ignored for generic)

# FastAPI Web API Configuration (API Phase 1)
# API server settings
API_HOST=0.0.0.0
API_PORT=8000
API_KEY=change-this-in-production-12345  # Change this for production!

# CORS settings (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Database
DATABASE_URL=sqlite:///checkpoints/workflow_runs.db

# File upload settings
UPLOAD_DIR=uploads
MAX_UPLOAD_SIZE_MB=20

# ============================================================================
# Docker Deployment Configuration
# ============================================================================
# When running with docker-compose, ensure the following variables are set:
#
# REQUIRED:
# - ANTHROPIC_API_KEY (for Claude Sonnet 3.5)
# - OPENAI_API_KEY (for GPT-5 Nano)
# - GOOGLE_API_KEY (for Gemini 2.5)
# - API_KEY (shared between frontend and backend, change for production)
#
# The following are set automatically by docker-compose.yml:
# - CORS_ORIGINS (includes http://localhost:3000,http://frontend:3000)
# - DATABASE_URL (sqlite:///app/checkpoints/workflow_runs.db)
# - UPLOAD_DIR (/app/uploads)
#
# To use:
# 1. Copy this file: cp .env.example .env
# 2. Add your LLM API keys (ANTHROPIC, OPENAI, GOOGLE)
# 3. Change API_KEY to a strong secret for production
# 4. Run: docker-compose up --build
