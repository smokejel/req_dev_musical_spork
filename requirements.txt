# Phase 0: Minimal dependencies for skills validation
langchain>=0.1.0
langchain-anthropic>=0.1.0
langchain-openai>=0.1.0
langchain-google-genai>=0.1.0  # Gemini models support (Phase 4.1)
python-dotenv>=1.0.0

# Phase 1: Foundation - Base architecture and Extract node
langgraph>=0.0.40          # Graph workflow orchestration
langgraph-checkpoint-sqlite>=3.0.0,<4.0.0  # Disk-based state persistence (Phase 4.1) - Upgraded to 3.0 for security fix (CVE-2025-64439)
pydantic>=2.5.0            # State schema validation
python-docx>=1.0.0         # DOCX document parsing
PyPDF2>=3.0.0              # PDF document parsing
pytest>=7.4.0              # Testing framework
rich>=13.0.0               # CLI output formatting

# Phase 4.2: Observability & Performance Monitoring
langsmith>=0.1.0            # LangSmith tracing and monitoring

# LangSmith Studio Support (Phase 6 - Studio Integration)
langgraph-cli[inmem]>=0.1.0  # Agent Server for LangSmith Studio interactive debugging

# FastAPI Web API Layer (API Phase 1)
-r requirements-api.txt
